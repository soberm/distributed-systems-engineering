server:
  port: 9090
  servlet:
    context-path: ""

spring:
  application:
    name: demo-service
  boot:
    admin:
      client:
        url: http://localhost:8123

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URL:http://localhost:8761}/eureka/
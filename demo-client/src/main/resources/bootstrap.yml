server:
  port: 9091
  servlet:
    context-path: ""

spring:
  application:
    name: demo-client
  boot:
    admin:
      client:
        url: http://localhost:8123
        
feign:
  hystrix:
    enabled: true